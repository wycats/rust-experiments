use spectrum_macros::frag;
use spectrum::{SimpleContext, StyledFragment};

struct Stringy {
    value: String,
}
impl Stringy {
    fn value(&self) -> &str {
        &self.value
    }
}
fn main() {
    let stringy = Stringy {
        value: "Niko".to_string(),
    };
    let value: StyledFragment<SimpleContext> = frag!({{{code}}});
}
