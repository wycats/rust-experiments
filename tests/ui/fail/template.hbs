use spectrum_macros::frag;
struct Stringy {
    value: String,
}
impl Stringy {
    fn value(&self) -> &str {
        &self.value
    }
}
fn main() {
    let stringy = Stringy {
        value: "Niko".to_string(),
    };
    {{{code}}}
}
